import { css } from 'styled-components'

const normal = css`
  font-family: 'Ubuntu-Regular';
  font-style: normal;
  font-weight: 300;
`

const bold = css`
  font-family: 'Ubuntu-Regular';
  font-style: normal;
  font-weight: bold;
`

export default {
  normal,
  bold,
}
